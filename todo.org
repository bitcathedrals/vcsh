* TODO for vc

* enhancements

* bugs

** commit, when there are no commits (like a feature branch) it breaks silently
** diff with one args should take a branch like it does, but also a range specification, can detect when arg has ".."
** integrate doesnt offer and do rebase 

* doc work
** show
** get
- no arg scenario: fast forward
- 1 arg scenario: use as remote name, do a fetch, and then log :upstream
- 2 arg scenario: set a upstream with arg1 = remote, arg2 = branch, set tracking, and fetch the remote
** patch
** pending
** list

** merge
** pull
** push

** cat
** rb


